spring:
  shell:
    interactive:
      enabled: false
  main:
    allow-circular-references: true
  mongodb:
    embedded:
      version: 3.4.11
mongock:
  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.otus.spring.library.mongock