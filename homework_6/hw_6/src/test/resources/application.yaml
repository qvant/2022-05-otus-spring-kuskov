spring:
  shell:
    interactive:
      enabled: false
  sql:
    init:
      mode: always
      data-locations: data.sql
  main:
    allow-circular-references: true
  jpa:
    hibernate:
      ddl-auto: none